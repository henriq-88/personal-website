{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": [".next/**", "out/**"]
    },
    "dev": {
      "cache": false
    },
    "lint": {}
  },
  "globalEnv": ["SENDGRID_API_KEY", "NEXTAUTH_URL", "NEXTAUTH_SECRET"]
}
